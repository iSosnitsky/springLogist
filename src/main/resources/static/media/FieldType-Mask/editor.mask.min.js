(function (c, b) {
    b.ext.editorFields || (b.ext.editorFields = {});
    var d = b.Editor;
    b.ext.editorFields.mask = {
        create: function (a) {
            a._input = c("<input/>").attr(c.extend({id: d.safeId(a.id), type: "text"}, a.attr || {}));
            a._input.mask(a.mask, c.extend({placeholder: a.placeholder || a.mask.replace(/[09#AS]/g, "_")}, a.maskOptions));
            return a._input[0]
        }, get: function (a) {
            return a._input.cleanVal()
        }, set: function (a, b) {
            a._input.val(b).trigger("change").trigger("keyup.mask")
        }, enable: function (a) {
            a._input.prop("disabled", false)
        }, disable: function (a) {
            a._input.prop("disabled",
                true)
        }
    }
})(jQuery, jQuery.fn.dataTable);
