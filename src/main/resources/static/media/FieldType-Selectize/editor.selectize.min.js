(function (b, d) {
    d.ext.editorFields || (d.ext.editorFields = {});
    var e = d.ext.editorFields;
    e.selectize = {
        _addOptions: function (a, c) {
            var b = a._selectize;
            b.clearOptions();
            b.addOption(c);
            b.refreshOptions()
        }, create: function (a) {
            var c = b("<div/>");
            a._input = b("<select/>").attr(b.extend({id: a.id}, a.attr || {})).appendTo(c);
            a._input.selectize(b.extend({valueField: "value", labelField: "label", dropdownParent: "body"}, a.opts));
            a._selectize = a._input[0].selectize;
            if (a.options || a.ipOpts)e.selectize._addOptions(a, a.options || a.ipOpts);
            return c[0]
        }, get: function (a) {
            return a._selectize.getValue()
        }, set: function (a, b) {
            return a._selectize.setValue(b)
        }, enable: function (a) {
            a._selectize.enable();
            b(a._input).removeClass("disabled")
        }, disable: function (a) {
            a._selectize.disable();
            b(a._input).addClass("disabled")
        }, inst: function (a) {
            return a._selectize
        }, update: function (a, b) {
            e.selectize._addOptions(a, b)
        }
    }
})(jQuery, jQuery.fn.dataTable);
